 - config / 配置
    - router-config / 路由配置
    - heyui-config / heyui配置
    - dict-config / 字典配置
    - tree-config / 树配置
    - autocomplete-config / autocomplete配置
    - category-config / category配置
    - menu-config / 系统菜单配置
